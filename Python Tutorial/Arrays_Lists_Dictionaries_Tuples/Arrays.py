from array import array
array1 = array('b')
print(array1)
#arrays in python can only take in one arguement and they have to be of char
#type (char is character type) and the characters can only one of the following:
                            #b, B, u, h, H, i, I, l, L, q, Q, f or d
